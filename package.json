{
  "name": "minibee-viewer",
  "private": true,
  "version": "0.8.0",
  "type": "module",
  "scripts": {
    "assetscompress": "node assetscompress.mjs",
    "tauri": "tauri",
    "test": "node --test",
    "test:rust": "cargo test --manifest-path src-tauri/Cargo.toml"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "esbuild": "^0.28.1"
  }
}
