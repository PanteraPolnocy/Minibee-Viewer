{
  "name": "minibee-viewer",
  "private": true,
  "version": "0.8.1",
  "type": "module",
  "scripts": {
    "assetscompress": "node assetscompress.mjs",
    "tauri": "tauri",
    "test": "node --test",
    "test:rust": "cargo test --manifest-path src-tauri/Cargo.toml",
    "release:notes": "node scripts/update-release-notes.mjs"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "esbuild": "^0.28.1"
  }
}
